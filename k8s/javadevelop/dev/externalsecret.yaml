apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: backendjava-db-password
  namespace: storefront-devjavaa
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: DB_PASSWORD
      remoteRef:
        key: onenode-dev/storefront-devjavaa/DB_PASSWORD
